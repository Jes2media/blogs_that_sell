# Redirect trailing slashes to non-trailing slash (301 permanent)
# This prevents duplicate URL indexing in Google Search Console
# Note: No force flag (!) so static files like index.html are served normally
/*/ /:splat 301
